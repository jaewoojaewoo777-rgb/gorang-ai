# 고랑AI 진행상황 로그

> 클로드코드의 "기억" 파일입니다.
> 세션 시작 시 이 파일을 읽고, 작업 후 맨 위에 새 기록을 추가하세요.
> 최신 작업이 맨 위로 오도록 합니다.

---

## [2026-07-14] 네이버 워커 진짜 원인 발견 — Railway 서비스에 공개 도메인 + NAVER_WORKER_SECRET이 통째로 없었음 (진행 중 저장)

- **한 일**: 어제부터 이어진 "배포직후 로그 안찍힘" + "새 리뷰 없음" 현상을 진단 로그(`[request]`/`[auth]`/`[browser]`, 커밋 `0cdaddc`)와 50초 타임아웃(`lib/naver.js`, 커밋 `b29213d0`)을 심어서 라이브로 추적. Railway `gorang-naver-worker` 서비스(프로젝트 "diplomatic-insight")를 직접 열어보니 두 가지가 원인이었음:
  1. **공개 도메인 자체가 없었음** — Settings → Networking에 도메인이 안 뜨고 "Generate Domain" 버튼만 있었음. `NAVER_WORKER_URL`(Vercel)이 가리키던 주소가 이 서비스와 연결이 안 되고 있었던 것으로 추정(예전 도메인이 사라졌거나 서비스가 재생성됐을 가능성).
  2. **`NAVER_WORKER_SECRET` 환경변수가 이 서비스에 아예 없었음** — Variables 탭이 "No Environment Variables". Railway 로그로 확인(`[auth] 거부 — secret설정됨:false`).
  - 참고로 같은 세션에서 `gorang-video-server`(별도 프로젝트 "faithful-courtesy")도 환경변수 없이 크래시된 걸 봤는데, 그건 프로젝트 착각(다른 Railway 프로젝트를 보고 있었음)이었고 실제 서비스는 변수 3개 다 정상 — 이건 문제 아니었음.
- **조치**:
  1. Railway에서 "Generate Domain" 클릭 → 새 주소 `https://gorang-naver-worker-production-a079.up.railway.app` 생성됨 → Vercel `NAVER_WORKER_URL`에 반영(처음에 `https://` 빼고 넣어서 "Failed to parse URL" 에러 한 번 남 → 재수정)
  2. 새 랜덤 시크릿 생성해서 Railway `NAVER_WORKER_SECRET`(신규 추가)과 Vercel `NAVER_WORKER_SECRET`(덮어쓰기) 양쪽에 동일하게 반영 지시 — **사용자가 지금 막 적용 중, 재배포+재테스트 결과 아직 확인 전**
- **다음 세션 시작하면 바로 할 것**: 
  1. "네이버 새 리뷰 확인" 재테스트 결과부터 확인. `[auth]` 로그 없이 `[browser]`/`[reviews]` 로그로 넘어가는지 볼 것
  2. 정상 진행되면 Railway `[reviews] 카드 목록 확보 N개` 숫자 확인(0이면 세션쿠키/셀렉터 문제로 원인이 또 바뀐 것, 6~10이면 `loadMoreCards`가 안 먹힌 것, 15면 dedup 문제)
  3. 여기까지 다 정상이면 그 다음에야 비로소 "네이버 새 리뷰 확인 반복 눌러서 43건 채우기" → 답변 게시(원탭) 테스트로 넘어갈 수 있음(둘 다 아직 한 번도 성공 못 함)
- **교훈**: 이번 세션의 진짜 배움은 "로그가 안 찍히면 코드 버그부터 의심하지 말고, 먼저 인프라 설정(도메인 존재 여부, 환경변수 존재 여부)을 스크린샷으로 직접 확인하라"는 것 — 코드는 처음부터 끝까지 문제 없었음.

---
---

## [2026-07-14] 배포직후 로그 안찍힘 이슈 — 원인 3후보로 좁히고 진단 로그 추가 (원인 확정은 아직)

- **한 일**: 어제 미해결로 남은 "loadMoreCards 배포 후 [reviews] 로그가 Railway에 전혀 안 찍힘" 현상, 코드 검토만으론 확정 불가해서 후보 3개로 좁힘 — ①배포 스왑 중 요청이 컨테이너까지 아예 안 들어감 ②`requireAuth` 인증 실패(실패해도 로그 없었음) ③`chromium.launch()`가 멈춤(launch 시작/완료 로그 자체가 없었음). 셋 다 기존 로그로는 구분 불가능해서, 다음 재현 시 바로 원인이 보이게 최소 진단 로그 + 안전장치를 추가.
- **변경된 파일**:
  - `gorang-naver-worker/server.js`(커밋 `0cdaddc`): 전체 요청 진입 로그(`[request] METHOD /path`), `requireAuth` 실패 사유 로그, 브라우저 `launch` 시작/완료 로그 추가
  - `gorang-ai/lib/naver.js`(커밋 `b29213d0`): 워커 호출에 50초 명시적 타임아웃(AbortController) 추가 — 이전엔 워커가 응답 없이 걸려있으면 Vercel이 55초에서 함수를 조용히 죽여서 `[naver/poll]` catch 로그조차 안 남을 수 있었음. 이제 타임아웃 시 "네이버 워커 응답 없음 (50초 타임아웃)" 형태로 명확히 로그 남음
- **결과**: 두 레포 모두 push 완료, Railway/Vercel 자동배포 트리거됨. 실제 원인 확정은 아직 — 배포 안정화 후 "네이버 새 리뷰 확인" 재현하면서 Railway `[request]`/`[browser]` 로그와 Vercel Runtime Logs(`/api/naver/poll`)를 같이 봐야 함
- **다음 할 일**: 배포 완료(1~2분) 후 재테스트 → ①Railway에 `[request] POST /reviews`가 찍히는지(안 찍히면 컨테이너 자체 미도달, Railway 인프라/타이밍 문제) ②찍히는데 `[auth]` 거부 로그가 있는지(시크릿 문제) ③`[browser] 신규 launch 시작`만 있고 `완료`가 없는지(브라우저 행) — 셋 중 어디서 끊기는지로 다음 조치 결정. 정상화 확인되면 밀린 과거 리뷰(43건 중 아직 안 채워진 분) 마저 채우고 답변 게시(원탭) 실사용 테스트로 이어갈 것.

---

## [2026-07-13] 네이버 리뷰 연동 드디어 실사용 성공 — 세션만료 + 날짜형식 버그 2개 추가로 잡음

- **한 일**: 지난 세션에서 고친 컬럼명 버그 배포 후에도 "새 리뷰 없음"만 계속 떠서 이어서 디버깅. 순서대로 두 개 원인을 더 찾아 고침.
  1. **네이버 세션쿠키 만료**: Railway Deploy Logs에서 `카드 목록 확보 0개`(16초 타임아웃) 확인 → `/api/naver/debug`에 `bodyText`(페이지 실제 텍스트) 추가해서 확인해보니 **"네이버 로그인이 필요한 기능입니다"** 모달이 떠 있었음(캡처해둔 쿠키가 만료됨). 크롬 확장으로 재연동(재로그인 + 연동코드 재발급)하니 카드 6~10개 정상 추출됨.
  2. **방문일 텍스트 → timestamptz 컬럼 파싱 실패**: 재연동 후에도 여전히 저장 안 됨 → `app/api/naver/poll/route.js`에 진단 로그 추가해서 Vercel Runtime Logs 확인, `22007 invalid input syntax for type timestamp with time zone: "2026. 6. 14(일)"` 에러 발견. 네이버가 방문일을 한국어 텍스트로 주는데 `create_time`(timestamptz) 컬럼에 그대로 넣고 있었음. `parseNaverDate()` 헬퍼로 정규식 파싱해서 `YYYY-MM-DD`로 변환 후 insert하도록 수정 → **드디어 실제 리뷰가 화면(완료/전체 탭)에 뜸.**
- **디버깅 방법 특기사항**: Railway "Deploy Logs"는 배포될 때마다 이전 배포가 "Removed"로 바뀌면서 그 로그 화면에 머물러 있으면 계속 옛날 로그만 보임 — 반드시 "Active"로 표시된 최신 배포를 열어서 봐야 함(이번에 헷갈려서 시간 좀 씀). Vercel Runtime Logs는 `console.log`/`console.error`가 바로 찍혀서 컬럼 관련 에러 원인 특정에 결정적이었음.
- **추가 변경**: 사용자 요청으로 "일반(긍정) 리뷰는 카톡 알림 안 감" 정책을 변경 — 이제 악성/주의/일반 전부 카톡 나감. 악성은 `sendBadReviewAlert`(gorang_bad_review_alert 템플릿, 침착한 톤), 주의·일반은 `sendReviewAlert`(gorang_review_alert 템플릿)로 통일. `app/api/naver/poll/route.js`, `app/api/reviews/poll/route.js`(GBP) 둘 다 수정 — 새 카카오 템플릿 등록 필요 없이 기존 템플릿 재사용.
- **변경된 파일**: `gorang-naver-worker/server.js`(디버그 엔드포인트에 bodyText 추가), `app/api/naver/poll/route.js`(날짜 파싱, 진단로그, 알림 정책), `app/api/reviews/poll/route.js`(알림 정책)
- **다음 할 일**: 지금은 배치(5개)씩만 처리하니 "네이버 새 리뷰 확인"을 여러 번 눌러서 43건 과거 리뷰를 다 채워야 함. 다 채워지면 답변 게시(원탭, `/api/naver/reply`) 실사용 테스트 필요 — 아직 안 해봄. 안정화되면 `/api/naver/debug`, 워커의 `/debug` 엔드포인트는 정리 고려.
- **⚠️→✅ 리뷰 목록 스크롤 안 되던 버그도 같은 세션에 이어서 수정**: 카드가 몇 개 안 될 땐 안 보이다가 늘어나면 아래로 스크롤이 안 먹힘. 원인은 `app/globals.css`의 `.app-shell`이 `min-height: 100vh` + `overflow: hidden`로 돼 있던 것 — min-height라 콘텐츠가 많아도 정확한 높이 기준이 안 잡혀서 안쪽 `flex:1 + overflowY:auto` 리스트 영역(`app/review/page.js`)이 제대로 스크롤 못 함. `height: 100vh`로 고정해서 해결(커밋 `b47efcae`). 개발자도구 콘솔로 실측 확인 완료(`app-shell: 890px fixed`, 리스트 컨테이너 618.5px≈컨텐츠619px) — **구조 자체는 정상, 지금 리뷰 6개가 딱 화면에 맞아서 스크롤할 게 없었던 것뿐**. 리뷰가 더 쌓이면(10개 이상) 실제로 내부 스크롤되는지 재확인 필요.
- **⚠️ "네이버 새 리뷰 확인" 반복해도 6개(진짜 5개+더미1개)에서 안 늘어나는 원인 발견, 부분 수정**: 사용자가 알려줘서 알아냄 — 우리 앱의 "더보기"(로컬 DB 페이지네이션, 3개씩)와 "네이버 새 리뷰 확인"(원격에서 새로 가져오기)은 별개 기능인데, 워커가 네이버 페이지 로드 시 **스크롤 없이 처음 렌더된 카드(6~10개)만** 가져오는 게 진짜 원인 — 매번 똑같은 최신 카드만 보여서 두 번째 클릭부터 전부 "이미 DB에 있음"으로 걸러짐(네이버 리뷰 리스트는 무한스크롤이라 43건 중 처음엔 일부만 로드됨). `gorang-naver-worker/server.js`에 `loadMoreCards()` 추가(스크롤 반복해서 카드 15개까지 확보, Vercel 55초 제한 고려해 30개 대신 15개로 제한) — 커밋 `d1ff603`, 배포 완료.
- **⚠️ 배포 직후 "네이버 새 리뷰 확인" 클릭이 Railway Deploy Logs에 전혀 안 찍히는 현상 발견(원인 미해결, 다음 세션 최우선 확인)**: 위 스크롤 수정 배포 후 새로고침까지 하고 버튼 눌렀는데, Railway Deploy Logs엔 `Starting Container`/`listening on 8080`만 있고 `[reviews]` 계열 로그가 하나도 없었음(즉 요청이 워커의 /reviews 핸들러까지 아예 안 들어간 것으로 보임). 배포 직후라 컨테이너 스왑 타이밍과 겹쳤을 가능성 있음 — **다음 세션에서: 배포가 확실히 "Active"로 안정된 지 1~2분 지난 뒤 다시 테스트, 그래도 로그가 안 찍히면 Vercel 쪽(`/api/naver/poll`) 로그부터 확인해서 worker 호출 자체가 됐는지(네트워크 에러 등) 볼 것.**
- **다음 세션 시작하면 바로 할 일(우선순위 순)**: ①위 "로그 안 찍힘" 현상부터 재현·원인 확인 ②정상화되면 "네이버 새 리뷰 확인" 반복 눌러서 리뷰 15개까지 채워지는지 확인 ③리뷰 늘어난 상태에서 스크롤 실사용 확인 ④답변 게시(원탭, `/api/naver/reply`) 테스트 — 아직 한 번도 안 해봄.

---

## [2026-07-12] 진짜 최종 원인 발견 — 존재하지 않는 컬럼 참조로 DB insert가 계속 조용히 실패

- **한 일**: 워커 성능 버그를 다 잡았는데도 리뷰가 안 뜨길래, Supabase에서 `reviews` 테이블 실제 컬럼 목록을 직접 조회(`SELECT column_name FROM information_schema.columns WHERE table_name='reviews'`)해서 확인.
- **찾은 것**: 코드에서 insert/update에 쓰던 `reply_status`, `existing_reply`, `korean_translation` 컬럼이 **실제 테이블에 아예 없었음**. 그래서 네이버 리뷰가 스크래핑까지 완벽히 성공해도 DB 저장 단계에서 매번 조용히 실패(`insErr` 체크만 하고 로깅을 안 해서 아무 흔적도 안 남았음) → 계속 "새 리뷰 없음"으로만 보였던 것.
- **실제 존재하는 컬럼**: `has_reply`(boolean), `reply_text`(text). `korean_translation`은 대응 컬럼 자체가 없어서 저장 안 하는 걸로 처리.
- **고친 파일**: `app/api/naver/poll/route.js`(insert), `app/api/naver/reply/route.js`(update), `app/api/reviews/poll/route.js`(GBP insert — 같은 버그라 같이 고침, GET 필터도 `has_reply` 기준으로), `app/review/page.js`(`normalizeReview`). 에러 로깅도 추가해서 앞으로 이런 문제가 또 생기면 바로 보이게 함.
- **⚠️ 다음 세션에서 바로 확인할 것**: 이 커밋(98f81c4b) 배포 후 "네이버 새 리뷰 확인" 재테스트 — 이번엔 진짜 리뷰가 뜰 가능성이 높음. 사용자가 급히 나가야 해서 라이브 확인은 다음에 이어감.

---

## [2026-07-12] 네이버 라이브 디버깅 계속 — 워커 성능 버그 3종 수정, 아직 리뷰 안 뜨는 중 (진행 중 저장)

- **한 일**: 위 항목 이후 계속된 라이브 테스트에서 504/네트워크오류/브라우저크래시를 순서대로 잡음. **가장 중요한 진단 도구는 Railway "Deploy Logs" 탭**(HTTP Logs 아님 — 거기엔 console.log 안 나옴)이었음.
- **찾아 고친 것들 (전부 `gorang-naver-worker` repo)**:
  1. **세션쿠키 401 버그 실사용 확인**: 지난 항목에서 고친 `gorang_session`/`gorang-session` 불일치 수정이 실제로 로그인 401을 없앴음(확인됨).
  2. **networkidle이 504의 주범**: `page.goto(..., {waitUntil:'networkidle'})`가 네이버 페이지의 끊임없는 백그라운드 통신 때문에 거의 항상 30초를 꽉 채워 기다렸음 → 리뷰 카드 셀렉터(`li[class*="Review_pui_review"]`)가 뜨는지 직접 `waitForSelector`로 기다리는 걸로 교체(3~4초로 단축, 로그로 확인).
  3. **★가장 컸던 원인★ — 카드 6개 파싱에 120초**: Deploy Logs로 `[reviews] 카드 목록 확보 6개, 3683ms` → `[reviews] 카드 파싱 전부 완료 123773ms` 확인. Playwright의 `innerText()`/`getAttribute()`는 요소를 못 찾으면 기본 30초까지 기다리는데, 카드마다 없을 수 있는 항목(결제정보 링크, 기존답글 등)에 타임아웃을 명시 안 해서 카드당 평균 20초씩 날아갔음 → 전체 셀렉터 호출에 `FAST_TIMEOUT=2000ms` 명시.
  4. **브라우저 인스턴스 크래시 후 복구 불가**: `browserContext.close: Target page, context or browser has been closed` 에러 발생(아마 Railway 메모리 부족으로 공유 Chromium 프로세스가 죽음) → 재배포 전까지 계속 실패하는 구조였음. `browser.on('disconnected', ...)`로 감지해서 다음 요청 때 자동 재시작하게 수정, `context.close()` 실패가 원래 에러를 가리는 것도 수정.
- **현재 상태 (이 시점 기준 미해결)**: 워커가 이제 빠르고 에러 없이 응답함("새 리뷰 없음", 초록 성공 메시지). **그런데도 실제 네이버 리뷰가 여전히 하나도 안 뜸.** 화면에 보이는 "KR 테스트유저" 리뷰는 예전 개발용 더미 데이터(음식점 컨셉 텍스트, 마음스튜디오와 무관)이지 네이버에서 온 게 아님. 의심되는 원인: 카드별 파싱에서 `extractReviewId`가 전부 null을 반환해 `if (!id) continue`로 스킵되고 있거나, 다른 필드 추출에서 개별 예외가 나서 조용히 스킵되고 있을 가능성(`catch (e) { console.error('[reviews] 항목 파싱 실패:', ...) }`로 카드별 에러는 잡히지만 전체 요청은 200 성공으로 끝남 — 이게 "새 리뷰 없음"으로 보이는 이유로 추정).
- **다음 할 일 (바로 이어서 할 것)**: Railway Deploy Logs에서 가장 최근 `/reviews` 요청 로그 확인 — `[reviews] 카드 목록 확보 N개` 숫자와 그 아래 `항목 파싱 실패` 라인들을 보면 정확한 원인이 나올 것. 그다음 원인에 맞게 셀렉터/로직 수정 → 재시도.
- **정리해둘 것(안정화되면)**: `gorang-naver-worker`의 `/debug` POST 엔드포인트, `app/api/naver/debug` GET 라우트 — 둘 다 임시 진단용, 나중에 지워도 됨.

---

## [2026-07-12] 네이버 라이브 테스트 디버깅: 세션쿠키 버그, 504, 셀렉터 최종 확정

- **한 일**: 실제 연동→폴링 테스트를 마음스튜디오 계정으로 반복 진행하며 발견된 문제 전부 수정.
- **찾아 고친 것들**:
  1. `app/api/reviews/poll/route.js`가 앱 전체가 쓰는 `lib/session.js`(쿠키명 `gorang_session`, 언더스코어) 대신 자체 iron-session(쿠키명 `gorang-session`, 하이픈)을 쓰고 있어서 **로그인해도 이 라우트만 항상 401**이 나던 오래된 버그 발견·수정. (지난 세션에 "리뷰 목록 안 보이는 버그 고쳤다"고 했던 게 사실 이 버그 때문에 반쪽짜리였음)
  2. `gorang-naver-worker/server.js`: 사용자가 직접 원문 텍스트(Ctrl+A/C)로 복사해준 실제 HTML 덕분에 대소문자 오타(`paymentinfo`→실제는 `paymentInfo`) 발견 — 이것 때문에 리뷰ID 추출이 계속 실패하고 "새 리뷰 없음"만 나왔음. 별점도 SVG 뒤 텍스트노드("별점5점" 형태)에서 정규식으로 추출 가능한 걸 확인해서 구현.
  3. `data-pui-click-code` 속성(`rv.replyedit`/`rv.replydelete`/`rv.replyfold`)이 class 해시(`pui__xxxxx`, 빌드마다 바뀔 수 있음)보다 훨씬 안정적인 셀렉터 기준이라는 것 확인, 전체 교체.
  4. 리뷰 43개를 한 번에 스크래핑+AI분석하다 Vercel 함수 시간제한 넘어서 504 발생 → `app/api/naver/poll/route.js`를 배치(8개씩) 처리 + AI분석 병렬화(Promise.all)로 변경, `hasMore`로 안내.
  5. 사용자 요청으로 리뷰 목록 UI를 페이지네이션으로 변경: 전체/대기/완료 각 탭 최신순 3개씩 로드, "더보기" 버튼으로 추가 로드. `GET /api/reviews/poll`에 `filter`/`limit`/`offset` 파라미터 추가, 정렬 기준을 `create_time`(문자열이라 정렬 안 됨, 네이버는 한글날짜라 더 문제)에서 `created_at`(DB 삽입 시각, timestamptz)으로 변경.
- **진단 방법 특기사항**: DevTools 접근 없이 스크린샷만으로 셀렉터 짜다가 여러 번 틀림(방문일 텍스트 위치 오판, 별점 SVG 오판 등) → `/api/naver/debug`, `gorang-naver-worker`의 `/debug` 임시 엔드포인트를 만들어 실제 HTML을 서버에서 직접 덤프해서 확인하는 방식으로 전환한 게 결정적이었음. **스크린샷보다 사용자가 Ctrl+A/C로 복사해준 원문 텍스트가 훨씬 정확함**(스크린샷은 OCR 오독 위험 있음 — 실제로 이번에도 결정적 대소문자 버그를 원문 텍스트 복사 후에야 찾음).
- **⚠️ 남은 것**: 라이브 테스트 아직 최종 성공 확인 전(이 로그 작성 시점 기준 배포 직후). 다음 세션에서 실제로 리뷰가 뜨는지, 답변 게시(원탭)까지 되는지 확인 필요. `gorang-naver-worker`와 `app/api/naver/debug`의 `/debug` 엔드포인트는 임시 진단용이라 안정화되면 제거 고려.
- **다음 할 일**: "네이버 새 리뷰 확인" 재테스트 → 성공하면 답변 게시(원탭) 테스트 → 셀렉터 안정 확인되면 `/debug` 엔드포인트들 정리.

---

## [2026-07-11] 네이버 워커 셀렉터 실측 반영 (마음스튜디오 실제 화면 캡처 2차례)

- **한 일**: 재우님이 마음스튜디오 계정으로 실제 스마트플레이스 사업자센터(`new.smartplace.naver.com`) 화면을 캡처해줘서, `gorang-naver-worker/server.js`의 추정 셀렉터를 실측값으로 교체.
- **확인된 사실**:
  - 리뷰 페이지 URL: `https://new.smartplace.naver.com/bizes/place/{placeId}/reviews?bookingBusinessId={bookingBusinessId}&menu=visitor` — placeId(경로)와 bookingBusinessId(쿼리)는 서로 다른 값이라 **둘 다 필요**함
  - 별점: "★5" 형태 텍스트로 렌더링 (붉은 별 + 숫자)
  - 기존 답글 있는 리뷰: "수정"/"삭제" 버튼. "수정" 클릭 → textarea 열림(저장 버튼도 "수정", 취소는 "닫기")
  - 미답글 리뷰: 트리거 클릭 없이 빈 textarea + "등록"(제출)/"닫기"(취소) 버튼이 바로 노출됨
  - 답글 글자수 제한: 최소 15자 ~ 최대 500자 (미만/초과 시 서버에서 막게 `/reply`에 검증 추가함)
- **설계 변경**: placeId/bookingBusinessId를 워커가 스스로 찾게 하는 대신, **확장이 사장님의 "리뷰" 관리 탭 URL에서 직접 파싱**해서 연동 시점에 전달하는 방식으로 바꿈 (자동탐색보다 훨씬 안정적).
  - `naver-extension/popup.js`: `chrome.tabs.query`로 열려있는 스마트플레이스 리뷰 탭 찾아 URL에서 두 ID 파싱 → `/api/naver/connect`에 같이 전송. 리뷰탭 안 열려있으면 에러 안내.
  - `naver_connections` 테이블에 `booking_business_id` 컬럼 추가 (schema 파일 갱신, **아직 Supabase 미실행**)
  - `lib/naver.js`, `app/api/naver/connect|poll|reply/route.js` 전부 bookingBusinessId 관통하도록 시그니처 수정
- **⚠️ 여전히 남은 것**: 리뷰 카드 컨테이너 자체의 실제 class/data속성은 아직 모름(DevTools 없이 화면 캡처만으론 한계) — 지금은 "방문일" 텍스트를 포함하는 div를 카드 경계로 추정하는 임시방편(`reviewItem` 셀렉터). 실제 쿠키 넣고 `/reviews` 돌려봐서 결과 비거나 이상하면 여기부터 의심할 것. 리뷰 고유ID도 작성자+방문일 조합으로 대체 중(불안정할 수 있음).
- **다음 할 일**: Supabase에 `booking_business_id` 컬럼 반영, Railway에 워커 배포, 실제 쿠키로 `/verify → /reviews → /reply` 라이브 테스트.

---

## [2026-07-11] 트립어드바이저 제거 + 네이버 리뷰 목록 표시되게 수정

- **한 일**: (이전 항목 "네이버 플레이스 연동 신규 착수" 직후 이어서 작업)
  1. 트립어드바이저 완전 제거: `app/api/tripadvisor/` 폴더 삭제, `app/review/page.js`(폴링버튼)·`app/settings/page.js`(연동해제)·`app/home/page.js`(홈 타일)·`app/connect/page.js`(안내 가이드)·`app/api/shop/route.js`(필드 노출)·`app/api/connections/disconnect/route.js`(해제 매핑)·`.env.example`(API 키)에서 전부 제거. **DB 컬럼(`tripadvisor_location_id` 등)은 안 지움** — 코드만 정리, orphan 컬럼으로 남음(안전).
  2. 리뷰 목록 표시 버그 수정: `reviews` 테이블이 두 가지 다른 컬럼셋으로 쓰이던 문제(아래 설명) 때문에 폴링으로 저장된 리뷰(트립어드바이저였던 것, 이제 네이버)가 화면에 안 보였음 → `app/review/page.js`가 `/api/reviews`(GBP 실시간조회, 옛 컬럼셋) 대신 `/api/reviews/poll` GET(이미 존재했지만 아무도 안 부르던 엔드포인트, 새 컬럼셋)을 보게 변경 + `normalizeReview()`로 필드명 정규화. 구글/네이버 리뷰가 한 목록에 통합되고, 답변 게시(`handlePublish`)는 `reviewId`가 `nv_` 접두사면 `/api/naver/reply`, 아니면 기존 `/api/reviews`로 자동 라우팅.
  3. 네이버를 트립어드바이저 자리에 배치: 설정/홈 화면에 "네이버 플레이스" 연동상태 표시(`naver_connected`, `/api/shop`에서 `naver_connections.session_status==='active'` 기준), 연동해제도 다른 채널과 동일한 UX로 지원(단, 실제로는 `naver_connections.session_status='revoked'`로 갱신 — users 테이블 컬럼 null처리가 아님).
- **⚠️ 컬럼 드리프트, 이번에 안 고친 부분**: `app/api/reviews/route.js`(GBP 답변게시·홈/통계 화면이 씀, 옛 컬럼셋: rating/review_text/has_reply/raw_data)는 그대로 둠. 즉 여전히 같은 테이블을 두 컬럼셋으로 나눠 쓰는 상태 — 오늘은 "폴링으로 저장된 리뷰가 화면에 보이게" 하는 것만 고쳤고, 전체 통합은 안 함. 나중에 필요하면 전체 리팩터링 고려.
- **다음 할 일**: 이전 항목의 미완료 6개(셀렉터 검증 등)와 동일. 추가로 실제 네이버 리뷰로 목록 표시가 잘 되는지, 답변 게시 라우팅이 잘 되는지 실사용 테스트 필요.

---

## [2026-07-11] 네이버 플레이스 연동 신규 착수 (세션쿠키 우회 방식) + CLAUDE.md 예외 승인

- **한 일**: 네이버가 플레이스 리뷰 공식 API를 안 줘서, 사장님이 1회 직접 로그인해 캡처한 세션쿠키로 Playwright 브라우저 자동화를 우회 연동하는 구조로 신규 설계·구현. CLAUDE.md의 "신규기능 보류" 규칙에 네이버 연동만 예외로 승인받아 진행(사용자 승인 2026-07-11).
- **변경/생성된 파일**:
  - `CLAUDE.md` — 네이버 연동을 "보류" 목록에서 예외 처리
  - `supabase-schema.sql` — `naver_connections` 테이블 + `users.naver_pairing_code`(+만료시각) 컬럼 초안 (⚠️ 아직 Supabase SQL Editor에서 실행 안 됨, 다음 세션에서 확인 필요)
  - `lib/crypto.js` — 신규. AES-256-GCM 암복호화 유틸(네이버 세션쿠키 저장용). `NAVER_SESSION_KEY`(32바이트 hex) 환경변수 필요
  - `lib/naver.js` — 신규. `gorang-naver-worker` 호출 wrapper (verifyNaverSession/getNaverReviews/postNaverReply)
  - `app/api/naver/pairing-code/route.js`, `app/api/naver/connect/route.js`, `app/api/naver/poll/route.js`, `app/api/naver/reply/route.js` — 신규 API 라우트. poll/analyzeReview은 TripAdvisor 패턴 그대로 재사용 (review_id는 `nv_` 접두사)
  - `app/review/page.js` — "네이버 연동하기"(연동코드 발급) / "네이버 새 리뷰 확인" 버튼 추가 (TripAdvisor 버튼과 동일 패턴)
  - `naver-extension/` — 신규 크롬 확장(별도 폴더). 사장님이 네이버 로그인 상태에서 버튼 클릭 → 세션쿠키만 캡처(비밀번호 미저장) → 연동코드로 짝지어 `/api/naver/connect`에 전송
  - `/home/firebat/gorang-naver-worker/` — 신규 별도 레포(로컬에만 존재, 아직 GitHub/Railway 미배포). Playwright로 세션쿠키 주입해 네이버 스마트플레이스 리뷰 스크래핑/답변게시. `mcr.microsoft.com/playwright` 베이스 이미지 Dockerfile 포함
- **⚠️ 미완료 (다음 세션 필수 확인)**:
  1. `supabase-schema.sql`의 네이버 관련 SQL, 실제 Supabase SQL Editor에서 아직 실행 안 함
  2. `gorang-naver-worker/server.js`의 `SELECTORS`/`URLS`는 전부 추정치 — 실제 네이버 스마트플레이스 화면 열어서 검증 안 됨. 배포 전 반드시 라이브 세션으로 `/verify → /reviews → /reply` 순서 테스트 필요
  3. `gorang-naver-worker`는 로컬에만 있고 아직 GitHub repo 생성·Railway 배포 안 됨
  4. `.env.local`에 `NAVER_SESSION_KEY`(openssl rand -hex 32), `NAVER_WORKER_URL`, `NAVER_WORKER_SECRET` 미설정
  5. `naver-extension/`은 개발자모드 사이드로드로만 테스트 가능 — 실사용자 배포하려면 Chrome 웹스토어 게시(자체 심사) 필요
  6. **발견한 기존 버그(네이버 작업과 무관, 별개 이슈)**: (a) `reviews` 테이블이 코드마다 다른 컬럼셋을 씀 — `app/api/reviews/route.js`는 옛 스키마(rating/review_text/has_reply/raw_data), `app/api/reviews/poll`·`tripadvisor/poll`은 신 스키마(star_rating/comment/review_type 등) 사용, `supabase-schema.sql`은 옛 스키마만 정의돼 있어 실제 운영 DB와 어긋남. (b) `app/review/page.js`가 마운트 시 `/api/reviews`(GBP 실시간 조회) 결과만 리스트에 표시 — `reviews` 테이블에 저장된 TripAdvisor/네이버 리뷰는 카톡 알림은 가지만 앱 화면 리스트/상세에는 안 나타남. 둘 다 고칠지 여부 사용자 확인 필요.
- **다음 할 일**: 위 미완료 6개 항목 순서대로 처리. 특히 2번(셀렉터 검증)은 재우님이 실제 네이버 계정으로 로그인한 상태에서 같이 확인해야 함.

---

## [2026-07-07] 결제 전략 변경: 토스 자동결제 보류 → 3개월 완전무료체험 + 수동전환, 약관 정비

- **한 일**: 토스페이먼츠 정기결제 모듈은 만들어뒀지만(아래 항목 참고), 라이브 전환 비용(가입비22만+연관리비11만=33만원)이 부담돼서 **당장은 라이브 전환 보류**. 대신 신규가입자 전원에게 **3개월 완전 무료체험**(카드 등록도 요구 안 함) 제공 후, 체험 종료 시점에 사장님이 **직접 카톡으로 연락해서 계좌이체로 유료 전환**시키는 방식으로 전략 변경. (Stripe/Airbnb의 "Do things that don't scale" 초기 수동 온보딩 사례 벤치마킹 근거로 결정 — 리서치 상세는 대화 로그 참고)
- **아직 미구현**: trial_ends_at 컬럼, 가입 시 자동 체험시작 스탬프, 설정/랜딩페이지의 체험 카운트다운·계좌이체 안내 UI, 결제전환용 SQL 템플릿 — **다음 세션에서 이어서 구현 필요**. 계좌번호(기업은행)도 아직 못 받음.
- **약관 정비(완료)**: 데이터를 나중에 AI 개선/통계/마케팅 리포트에 쓸 수 있도록 `app/terms/page.jsx` 7조(지식재산권), `app/privacy/page.jsx` 2조(수집·이용목적)에 "이용자 콘텐츠·이용데이터(사진/영상, AI답변 초안+수정본, 게시성과)를 익명·통계처리하여 AI개선/통계/마케팅자료에 활용 가능" 문구 추가. 식별 가능한 형태 제3자 판매는 금지 명시.
- **다음 할 일**: ①기업은행 계좌번호+예금주명 받기 ②trial_ends_at 스키마+가입플로우 구현 ③설정/랜딩 UI에 체험카운트다운+계좌이체 안내 ④유료전환용 SQL 템플릿 정리 ⑤(보류) 토스 라이브 전환은 나중에 사용자 규모 커지면 재검토

---

## [2026-07-07] 결제 모듈(토스페이먼츠 정기결제) 신규 구현

- **한 일**: 결제/구독 인프라를 처음부터 구현. PG=토스페이먼츠, 정기결제(빌링키 자동청구), 정산계좌=기업은행(가맹점 가입 시 사용자가 직접 등록). 해지는 "다음 결제일까지 이용 후 종료" 방식.
- **변경/생성된 파일**:
  - `lib/toss.js` — 빌링 API 래퍼(issueBillingKey, chargeBilling), PLAN_PRICES
  - `app/billing/page.js` — 요금제 선택 + 토스 SDK(`https://js.tosspayments.com/v1/payment`) `requestBillingAuth('카드', ...)` 카드 등록 플로우
  - `app/api/payment/billing/confirm/route.js` — 카드등록 콜백(GET) → 빌링키 발급 → 첫 결제 청구 → users/payments 반영
  - `app/api/payment/charge/route.js` — Vercel Cron 전용(GET, `CRON_SECRET` 헤더검증), 매일 `next_billing_at` 지난 활성 구독 정기청구 + 해지예약분 기간종료 처리
  - `app/api/payment/cancel/route.js` — 구독 해지 예약(POST, `cancel_at_period_end=true`)
  - `app/api/shop/route.js` — GET 응답에 `id`, `plan`, `subscription_status`, `next_billing_at`, `cancel_at_period_end`, `card_company`, `card_last4` 추가
  - `app/settings/page.js` — 하드코딩된 "현재 플랜: 스탠다드" 제거, 실 DB값 표시 + 플랜변경/구독해지 버튼
  - `app/page.js` — PLANS 배열에 프로(129,000원) 추가
  - `vercel.json` — 신규, cron `0 1 * * *`(매일 KST 10시)로 `/api/payment/charge` 호출
  - `supabase-schema.sql`, `.env.example` — 참고용 스키마/환경변수 주석 추가 (실제 DB 반영은 사용자가 SQL Editor에서 직접 실행 필요)
- **결과**: 코드 작성 완료, `node --check`로 신규 API/lib 파일 문법 검증 통과(이 WSL엔 리눅스 node 없어 `next build` 실행 불가 — [[ffmpeg-no-sudo-wsl]] 참고).
- **실제 테스트 완료(같은 날 후속)**: Supabase SQL 실행(2단계로 나눠 실행 — 한번에 붙여넣었을 때 일부 텍스트가 잘려 문법 에러 발생했었음, 나눠서 재실행 후 성공) → Vercel에 토스 테스트키(`test_ck_`/`test_sk_`) + `CRON_SECRET` 등록 → 배포 완료 → `/billing`에서 카드 등록 시도. **1차 실패**: 임의로 지어낸 카드번호 입력 → `INVALID_CARD_NUMBER` (테스트모드라도 카드번호는 실제 카드사 BIN을 통과해야 함, 완전 무작위 숫자는 안 됨). **실제 소유 카드번호로 재시도 → 성공.** 카드등록→빌링키발급→첫결제청구→`/settings`에 플랜/다음결제일 정상 표시까지 전체 플로우 확인됨.
- **다음 할 일**:
  1. (선택) 정기결제 cron이 실제로 매일 도는지는 `next_billing_at`이 돌아올 때(가입 후 한 달 뒤) 확인 필요 — 급하면 Supabase에서 `next_billing_at`을 임의로 과거 날짜로 바꾸고 `/api/payment/charge`를 수동 호출(`Authorization: Bearer <CRON_SECRET>` 헤더)해서 즉시 검증 가능
  2. 테스트 통과 확인됐으니 실사용자 오픈 전 토스 라이브 키로 교체(사업자 가맹심사 필요) — 아직 진행 안 함
  3. (범위 밖) 플랜별 기능 제한(영상 개수 등) 강제는 아직 미구현 — 필요시 별도 작업

---

## [2026-07-01] 틱톡 Direct Post audit 2차 반려 — 원인=데모영상(코드 아님)

- **반려 접수**: 오늘 12:44 메일, ref `20260626153213` (6/26 재제출분). "App Details에서 사유 확인" + 지원팀 회신(ref `20260620155132` 관련)이 사유 명시.
- **사유(지원팀 원문)**: "does not comply with UX Guidelines. 앱 **로그인부터** → 틱톡에 **콘텐츠 게시 성공**까지 완전한 end-to-end 흐름을, 모든 필수 UX 단계가 보이게 데모영상으로 제출하라." → **코드 문제 아님, 데모영상 문제.**
- **코드 검증 완료**: `app/video/page.js` L1702~1830 Direct Post UI에 필수 UX 요소 **전부 구현됨** — 계정표시(L1717), 공개범위 기본 미선택+직접선택(L1729), 상호작용 토글(L1748), 상업콘텐츠 토글 기본OFF(L1768), 내브랜드/브랜디드(L1777), 음원동의 문구+링크(L1820). **수정 불필요.**
- **다음 할 일 (데모영상 재촬영, 컷 없이 한 흐름)**: ①gorang-ai.com 구글**로그인부터** ②영상생성 ③틱톡 선택 ④계정표시 ⑤공개범위 미선택→직접선택 ⑥상호작용 토글 ⑦상업콘텐츠 ON→내브랜드 체크(음원동의 문구 렌더 트리거) ⑧음원동의 문구 화면노출 ⑨게시버튼→성공 ⑩**틱톡 앱/프로필서 실제 게시물 확인**(비공개여도 OK). **①로그인·⑩틱톡실게시가 이전 영상 핵심 누락분 추정.**
- **App Details 확인**: 반려문구는 일반 가이드라인 링크뿐. **결정적 발견** = App review 업로드칸에 붙어있던 파일이 `gorang_tiktok_demo_30fps.mp4`(Downloads, 다른 파일)였음 → 지난 반려는 이 허술한 영상 탓. **`틱톡데모_영어자막.mp4`(바탕화면/고랑, 11MB, 2:43)는 제출된 적 없었음.**
- **`틱톡데모_영어자막.mp4` 프레임 전수검증 = 필수 10단계 전부 포함**: 앱 로그인(gorang-ai.com→구글OAuth) → 틱톡OAuth → 영상생성 → 계정표시 → 공개범위 미선택→직접선택 → 상호작용토글 → 상업콘텐츠 ON(내브랜드=프로모션) → 음원사용확인 → 게시→업로드 → **틱톡 프로필 실게시 확인**(36초전·🔒비공개·프로모션·AI라벨). 영어자막 5개 내레이션.

## [2026-07-01] 입력 게이트 mix 모드 확장 — 영상 프레임 채점 + 해상도/흔들림

- **배경**: mix 모드(사진+영상)에서도 점검 필요. 단 "영상에 자막만 입히려는 사람"도 있어 경고형 유지(막지 않음).
- **구현**(`app/video/page.js`, 클라이언트만 / 서버·API 재사용):
  - `analyzeVideo(file)` 헬퍼: 영상에서 25/50/75% 3프레임 추출 → **중간 프레임(50%)을 base64로 채점용**, 인접 프레임 48×48 그레이 평균차로 **움직임(흔들림) 지표** 산출, `videoWidth/Height`로 **해상도** 확인.
  - `runMixCheck`: mixItems 순회(최대6) — 사진은 resizeToBase64, 영상은 analyzeVideo → 모아서 `/api/photos/check`(Vision 4기준 채점) 한 번 호출 → 영상엔 **해상도<720p / 움직임>18** 조건 결합해 verdict 보정.
  - mix UI에 "🔍 AI 점검(선택)" 버튼 + 사진📷/영상🎬 구분 결과. "자막만 입히려면 그대로 진행 OK" 안내.
  - mixItems 추가/삭제/순서변경 시 결과 리셋.
- **한계/튜닝포인트**: 흔들림 임계값(18)·해상도컷(720p)은 실데이터로 조정 필요. 진짜 흔들림 중 "각 프레임은 선명한데 지터"만 있는 경우는 프레임 모션블러로만 잡혀 놓칠 수 있음(움직임 지표가 보완).
- **다음**: 배포 후 실영상으로 임계값 검증.

## [2026-07-01] 입력 게이트(AI 사진 품질 점검) 코드 구현 — 경고형

- **개념**: 홍보영상 품질 하한선 확보. 올린 사진을 Claude Vision이 4개 기준(선명도/밝기/정돈도/수평각도) 0~5점 채점, 하나라도 3점 미만이면 재촬영 권고. **강제 차단 아님(경고형)** — 표시돼도 업로드는 계속 됨(실손님 사진 막으면 이탈).
- **주의**: 원칙상 유료고객 검증 후 자동화가 맞으나(수동=재우가 온보딩때 Claude로 직접 검수), 사용자 요청으로 지금 코드화. 프롬프트가 이미 검증돼 있어 나중 코드화와 동일 결과라 리스크 낮음.
- **구현**(기존 캡션 Vision 패턴 재사용):
  - `lib/ai.js` `checkPhotos()` 추가 — Anthropic `claude-sonnet-4-6`, base64 이미지블록, JSON 배열 응답 파싱 + 방어적 정규화(점수 하나라도 <3 → retake 강제). 최대 6장.
  - `app/api/photos/check/route.js` 신규 — 세션 인증 후 checkPhotos 호출.
  - `app/video/page.js`: 모듈스코프 `resizeToBase64`(768px 압축) 헬퍼 + 상태 `photoCheck`/`checkingPhotos` + `runPhotoCheck` 핸들러 + 사진모드 프리뷰 아래 "🔍 AI 사진 점검(선택)" 버튼 + 결과 요약(✅/⚠️ + 이유). 사진 추가/삭제 시 결과 리셋.
  - env 추가 불필요(ANTHROPIC_API_KEY 기존 사용). mix 모드는 이번 범위 외(photos 모드만).
- **검증**: JSX 균형 정독. 로컬 Linux node 없어 → Vercel 자동빌드로 최종검증.
- **다음 할 일**: 배포 후 실사진으로 점검 정확도 확인(임계값 3점 적정한지), 온보딩 3명 돌려 데이터 축적. 필요시 mix 모드 확장.

## [2026-07-01] 영상 만들기에 '사진 잘 찍는 법' 촬영 가이드 토글 추가

- **배경**: 자동화 영상이 먹히려면 품질 하한선 필요. "입력 통제 = 출력 통제". Vision 자동게이트는 검증 전 신기능이라 보류(수동=온보딩 때 재우가 Claude로 사진 직접 검수), **촬영 가이드는 콘텐츠라 지금 구현**.
- **구현**(`app/video/page.js`): 사진 업로드존 ↔ 업로드 플랫폼 사이에 **중첩 접이식 가이드** 추가. `mode==='photos'||'mix'`(사진 쓰는 모드)에서만 노출.
  - 모듈스코프 `SHOOT_GUIDE`(카페/펜션·숙소/식당 × 5샷 레시피) 상수 추가
  - 상태 `guideOpen`(메인 토글), `guideBiz`(업종별 펼침) 추가
  - 내용: 기본3원칙(자연광·수평·주인공 하나) + 업종별 "이 5장만" 토글 + ❌하지마 + "좋은 4~5장이면 충분"
  - 기존 인라인스타일/색(#1D9E75 등) 그대로 맞춤([[feedback-follow-existing-ui-patterns]] 준수)
- **검증**: JSX 균형 정독 확인(1139~1178). 로컬 Linux node 없어 next build 미실행 → **Vercel 자동빌드로 최종검증**.
- **다음 할 일**: GitHub push → Vercel 배포 후 실제 화면에서 토글 열림/업종 펼침/모바일 레이아웃 확인. (입력게이트 코드화는 유료고객 검증 후.)

## [2026-07-01] 틱톡 Content Posting API audit 3차 재신청 제출 완료 ✅

- **좋은 영상으로 재제출 완료** (developers.tiktok.com/application/content-posting-api, 4스텝 폼):
  - Step1 일반정보: 마음스튜디오 / gorang-ai.com / 조직 소개(영어)
  - Step2 API client: **App ID `7647432311495575572`** / 목표 설명 / 일일 게시자 "Less than 100" + 추정근거(초기 베타)
  - Step3 서류: **`틱톡데모_영어자막.mp4` 업로드**(요구 4항목 ①인증 ②게시화면 ③게시후 ④가이드라인 다 커버) + 저장 API필드 답변(open_id·access/refresh_token·expires_in·display_name·publish_id·status — 실제 코드 `callback/route.js`·`upload/tiktok/route.js` 기준)
  - Step4 검토 → 제출
- **결과: "Application ... has been submitted! 2~4주 내 회신."** Manage apps에서 상태 확인.
- **다음 할 일**: ~7월 중순~하순 심사결과 대기. 승인 시 SELF_ONLY 해제→공개게시 가능. (선택 보강: 재반려 시 맨앞 로그인 구간에 "Step 1: Log in" 자막 추가한 버전으로 재촬영.)

## [2026-06-30] 자막 순차 표시 수정 + 50MB 업로드 우회(압축) + 틱톡 audit 안내

- **자막이 한번에 다 뜨는 문제 수정** (gorang-video-server/server.js): `generateASSMixed`가 캡션 문단 전체를 한 클립에 통째로 띄우던 걸, `chunkText`/`rebucketParts` 헬퍼로 **문장(없으면 단어) 단위 덩어리로 잘라 앞 2/3 구간에 시간순 배치**(최소 1.3s/덩어리, 세로 maxChars 26·가로 46). 한·영 자막을 같은 타이밍으로 짝지어 함께 넘어가게. 실제 캡션 시뮬레이션 결과 4덩어리×3.2s로 자연스럽게 분할 확인. **로컬 커밋 b5d5372, 푸시 대기(Railway 배포)**.
- **50MB 업로드 우회**: Supabase Free 50MB 한계 때문에 막힌 아르떼뮤지엄 2(64MB)·5(49)·6(46)을 imageio-ffmpeg(pip, sudo없이)로 H.264 CRF28 압축 → 각 10~13MB. `바탕화면/제주핀-큐레이션/영상샘플/아르떼뮤지엄N_압축.mp4`. Pro 미업그레이드 유지([[gorang-video-plan-gating]]).
- **틱톡 업로드 실패**(`unaudited_client_can_only_post_to_private_accounts`): 버그 아님. Direct Post audit 미승인이라 비공개(SELF_ONLY)로만 게시 가능. 재심사 6/27 제출, 2~4주 대기. 당장은 공개범위 '비공개' 선택 시 업로드됨(이후 틱톡 앱에서 수동 공개).
- **유튜브 잘못된 채널 연동 수정 + 전 플랫폼 연동해제 통일**: 제주핀 로그인인데 업로드가 고랑AI 채널로 가던 문제. 원인=OAuth `prompt:'consent'`만 써서 채널 재선택 불가 + 구글 연동해제 수단 없음.
  - `lib/google.js` getAuthUrl forceSelect 옵션(`select_account consent`), `/api/auth/google?reauth=1` 지원 (연동하기 시 채널 선택 가능)
  - **공통 연동해제 API** `/api/connections/disconnect` (POST {platform}): google/instagram/tiktok/line/tripadvisor 컬럼만 null. google은 토큰·GBP만 비우고 google_id/email 유지→로그인 안 풀림. DB에 없는 컬럼은 건너뜀(select * 후 `in user` 체크)
  - **설정페이지 전 플랫폼 통일**: 틱톡식 '연동해제'(빨강)/'연동하기'(초록) 버튼을 CONNECTIONS 배열로 5개 플랫폼 일괄 렌더. 기존 하드코딩 행·'채널 변경' 커스텀 버튼·'계정 추가 연동' 링크 제거
  - `/api/shop` google_connected를 `!!google_access_token` 기준으로 변경(연동해제 시 상태가 풀리도록)
  - 채널 변경 방법: 설정→YouTube 연동해제→연동하기→구글 채널 선택화면에서 제주핀 선택
- **영상서버 자막수정 푸시 완료** (b5d5372 → Railway 배포).
- **다음 할 일**: gorang-ai 푸시 → ① 새 영상으로 자막 순차표시 확인 ② 설정→YouTube "채널 변경"으로 제주핀 채널 재연동 후 업로드 테스트.

## [2026-06-30] 브랜드 로고 교체(나뭇잎🌿→고랑AI 워드마크) + 영상만들기 미연동 채널 선택 차단

- **로고 교체**: 임시 나뭇잎 이모지(🌿)를 정식 고랑AI 워드마크 이미지로 전부 교체
  - 신규 에셋: `public/logo.png` (1024×1024, 흰 배경 워드마크 "고랑AI · 제주 자영업 성장 파트너")
  - `app/page.js` 3곳: 헤더(로고 클릭 시 `/`로 이동), 푸터(어두운 배경→흰 라운드박스 래핑), 로딩화면(초록 배경→흰 라운드박스 래핑)
  - 영상페이지 진행메시지 끝 장식 🌿는 로고 아님이라 유지
  - privacy/terms의 "고" 그래디언트 박스 로고는 나뭇잎 아니라 이번 범위 외
- **영상만들기 연동 가드**: 연동 안 된 채널은 업로드 플랫폼 칸에서 선택 불가
  - `app/api/shop/route.js`: 응답에 `facebook_connected`, `line_connected` boolean 추가(토큰 원문은 비노출). youtube=google_connected / instagram=instagram_user_id / tiktok=tiktok_open_id는 기존값 재사용
  - `app/video/page.js`: 모듈스코프 `platformConnected(id, shop)` 헬퍼 추가, 마운트 시 `/api/shop` fetch→`shop` 상태 + 미연동 채널은 선택목록에서 자동 제거. 미연동 행은 흐리게(opacity 0.55)+`not-allowed`+체크박스 disabled+"🔗 연동 필요" 배지, 클릭 시 `/connect`로 안내. 샤오홍수는 네이티브 공유라 항상 선택 가능
- **변경 파일**: `public/logo.png`(신규), `app/page.js`, `app/video/page.js`, `app/api/shop/route.js`
- **결과**: 코드 수정 완료. 로컬에 Linux node 없어 `next build` 미실행(정상 흐름=Vercel 자동빌드). 코드 정독으로 JSX/문법 확인.
- **다음 할 일**: GitHub push → Vercel 배포 후 실제 화면에서 로고 표시(특히 어두운 배경)·플랫폼 선택 차단 동작 확인

## [2026-06-27] 틱톡 Direct Post 재신청(Reapply) 제출 완료 + 워터마크 제거

- **틱톡 audit 재신청 제출 완료** (Content Posting API - Direct Post)
  - 데모영상 검토: 새로 찍은 `틱톡데모수정1.mp4`가 반려 사유 3가지(공개범위 선택·상업콘텐츠 공개·실제 프로필 게시)를 전부 충족함을 프레임 분석으로 확인. 게시 결과물에 "프로모션 콘텐츠 · AI 생성 라벨 · 🔒비공개"까지 정상 표시.
  - **영어 자막 5개 박은 보강본 제작** → `바탕화면/고랑/틱톡데모_영어자막.mp4` (11MB, 원본 199MB는 50MB 제한 초과라 압축본 사용). 자막: OAuth / 공개범위 수동선택 / 상업콘텐츠(Your Brand) / 음원동의 / 프로필 직접게시.
  - 제출 폼 "API response data fields saved in DB" 답변 작성(open_id·access/refresh token·expires_in·display_name·publish_id).
  - 제출 완료 → **2~4주 심사 대기**.
- **영상 워터마크 "MADE BY Gorang AI" 시스템 전체 제거** (사용자 요청)
  - `gorang-video-server/server.js`의 ASS Watermark 스타일+Dialogue+미사용 변수 제거 → push 완료(Railway 자동배포). 앞으로 생성되는 모든 영상에 워터마크 안 박힘.
  - 기존 데모영상의 워터마크는 녹화 픽셀에 baked-in이라 미제거(작고 반투명+틱톡UI와 겹침, 심사 무관이라 그대로 제출).
  - rebase 충돌 정리: 리모트의 제목 폰트크기·타이밍·Playfair 폰트 개선은 유지하고 워터마크 조각만 제거.
- 변경 파일: `gorang-video-server/server.js` (워터마크 제거). Direct Post 코드는 [2026-06-26] 항목 참고.
- 다음 할 일:
  1. 틱톡 audit 결과 대기 (승인 시 공개 게시 가능, 반려 시 사유 재분석)
  2. (선택) 워터마크 없는 깔끔한 데모 원하면 영상 재생성 후 재촬영

---

## [2026-06-26] 틱톡 Direct Post 구현 (audit 반려 대응)

- 배경: TikTok Content Posting API **Direct Post audit 반려** (ref 20260620155132). 사유는 일반 가이드라인 문구뿐.
  - 원인 진단: 기존 코드/데모가 **Direct Post가 아니라 Inbox(드래프트) 업로드** 흐름이었음.
    - `lib/tiktok.js`가 `publish/inbox/video/init/`만 사용, `queryCreatorInfo()`는 정의만 하고 미사용
    - 공개범위 선택/상업콘텐츠 공개/음원동의 등 Direct Post 필수 UI 전무
- 한 일: Direct Post 실제 구현 (UX 가이드라인 준수)
  - `lib/tiktok.js`: `directPostTikTokVideo()` 추가 — `publish/video/init/` + `post_info`(privacy_level, disable_comment/duet/stitch, brand_content/organic_toggle, is_aigc)
  - `app/api/tiktok/creator-info/route.js` (신규): 게시 화면 진입 시 creator_info 조회(공개범위 옵션/닉네임/상호작용 가능여부)
  - `app/api/upload/tiktok/route.js`: `directPost` 플래그 시 Direct Post 분기(미선택 시 기존 Inbox 폴백), Branded+비공개 차단, 비공개 게시 안내 note
  - `app/video/page.js`: 틱톡 게시 UI 추가 — 공개범위 셀렉트(기본 미선택), 상호작용 허용 토글(기본 미체크+계정 비활성 반영), 상업콘텐츠 공개 토글(내 브랜드/브랜디드, 최소1개), AIGC 라벨, 음원/브랜디드 동의 고지, 설정 미완료 시 업로드 버튼 비활성
- 변경된 파일: `lib/tiktok.js`, `app/api/tiktok/creator-info/route.js`(신규), `app/api/upload/tiktok/route.js`, `app/video/page.js`
- 결과: 코드 작성 완료. 로컬 WSL에 node 미설치라 빌드 미실행 → **Vercel 배포 빌드로 검증 필요**. 기능 테스트(실제 게시) 미완.
- 참고: audit **통과 전에는 privacy_level이 SELF_ONLY(비공개)로 강제**됨. 공개 게시는 audit 승인 후.
- 다음 할 일:
  1. push → Vercel 빌드 확인
  2. 프로덕션에서 틱톡 연동 후 Direct Post 실제 게시 테스트(비공개로 올라가는지)
  3. 그 흐름으로 **데모 영상 재촬영**(OAuth→공개범위선택→상업콘텐츠토글→게시결과) → 개발자포털 **Reapply**

## [2026-06-20] 랜딩페이지·OAuth 쿠키·채널 연동창·영상 SIGKILL 수정 ✅

- 한 일:
  1. **서비스 소개 랜딩페이지** — 비로그인 시 `/`에 마케팅 소개 페이지 노출(히어로→핵심수치→기능3종→사용법3단계→요금제→하단CTA). 로그인 완료 사용자는 `/home` 자동 리다이렉트, 미등록은 `/register`
  2. **Google OAuth 세션 쿠키 누락 완전 수정** — Next.js 14 Route Handler에서 `getSession()`(cookies()) 및 `getIronSession(req,res)` 모두 redirect 응답에 Set-Cookie가 누락되던 문제를, `sealData()`로 직접 암호화 후 `response.cookies.set()`으로 박는 방식으로 해결. (이후 사용자/타 작업이 `COOKIE_DOMAIN` 옵션, no-store, 디버그 라우트 추가)
  3. **영상 페이지 BGM 패널 버그** — BGM 변경 버튼 클릭 시 `setModifyMode(null)` 제거 → 생성 중에도 패널 유지("생성 중..." 표시), 완료 후 자동 닫힘(handleGenerate 끝에서 setModifyMode(null))
  4. **채널 탭 연동 안내 창** — 틱톡/트립어드바이저/레드노트를 "심사중/준비중"→"연동하기"로 변경. connect 페이지에 `?flow=` 기반 GUIDES 가이드 3종 추가:
     - 틱톡 → `/api/auth/tiktok` 실제 OAuth (라우트 기존 존재)
     - 트립어드바이저 → 비즈니스 관리 센터 안내 (공식 API 제한)
     - 레드노트 → 샤오홍슈 반자동 업로드 안내 (업로드 API 없음)
  5. **아이콘 정비** — 페이스북: 파란책📘→실제 페북 로고 SVG(파란 사각형+흰 f) / 레드노트: 📕→🇨🇳
  6. **🔴 영상 ffmpeg SIGKILL(OOM) 수정** — 6/19에 화질 개선하며 바꾼 `preset medium`이 4K 스케일업+거대 자막 렌더와 겹쳐 Railway 메모리 초과→OOM 강제종료. `medium`→`veryfast` 복원(검증된 설정). 화질은 CRF가 결정하므로 화질개선(CRF 26→20)은 유지
  7. **자막 폰트 확대(6/19~20)** — 제목 ×4(120→480 세로), 부제목 ×4(80→320), 본문 ×2(64→128), 외국어 ×2(50→100). server.js PlayRes 4K 기준

- 변경된 파일:
  - `app/page.js` (랜딩페이지 전면 작성)
  - `app/api/auth/google/callback/route.js` (sealData + cookies.set)
  - `app/video/page.js` (BGM 패널 유지)
  - `app/home/page.js` (채널 7종 + href 라우팅 + 페북 SVG + 🇨🇳)
  - `app/connect/page.js` (GUIDES 틱톡/트립어드바이저/레드노트 플로우)
  - `[video-server] server.js` (preset veryfast 복원, CRF 20, 폰트 크기)

- 결과:
  - gorang-ai: GitHub main 푸시 완료(원격 충돌은 rebase로 병합). Vercel 자동 배포
  - gorang-video-server: GitHub main 푸시 완료. Railway 자동 배포
  - ⚠️ 로컬 빌드 미실행(이 WSL bash에 node 없음) → Vercel/Railway 빌드가 검증 게이트
  - ✅ 큐레이션 계정 로그인 정상화(Google Cloud Test users 추가 후)

- 다음 할 일:
  - Railway 재배포 후 영상 생성 SIGKILL 재발 여부 확인 (재발 시 CRF 한 단계↑)
  - CRF 20 + veryfast에서 파일 용량 초과 재발 여부 모니터링(과거 CRF 18에서 초과 이력)
  - 틱톡 OAuth 프로덕션 키 실제 업로드 테스트
  - Google Cloud Console: GOOGLE_REDIRECT_URI 등 환경변수 정상 확인됨(빈값이던 것 채움)

---

## [2026-06-19] UI 개선·버그 수정·4K 영상·속도 최적화 ✅

- 한 일:
  1. **Google OAuth 프로덕션 게시** — 테스트 유저 추가 후 OAuth 동의화면 프로덕션 배포
  2. **홈 채널 목록 확장** — YouTube, TikTok, Instagram, Facebook, TripAdvisor, RedNote(🇨🇳) 7개로 확장
  3. **홈 연동 상태 배지** — 연동됨 ✓ 초록 배지, 미연동 시 "채널 연동하러 가기" 안내 카드
  4. **홈 스마트 버튼** — 1개 이상 연동되면 "영상 만들러 가기 →" 버튼, 미연동이면 안내 카드
  5. **YouTube 연동 분리** — ytConnected(구글OAuth)와 gbConnected(GBP위치) 분리, Google 로그인만 해도 YouTube ✓ 표시
  6. **스플래시 스마트 라우팅** — 이미 로그인된 상태면 /home 자동 이동
  7. **connect 페이지** — 구글 이미 연동됐으면 "✅ 연동됨 — 홈으로 가기 →" 버튼 표시
  8. **영상 step3 수정 패널** — "✏️ 자막 수정" / "🎵 BGM 변경" 버튼 추가
  9. **워터마크** — 영상 우하단에 "MADE BY Gorang AI" 항상 추가
  10. **이모지 폰트** — fonts-noto-color-emoji 설치로 □ 버그 해결
  11. **순수 비디오 자막 버그** — hasPhotos 로직으로 비디오 전용 모드에서 자막 정상 표시
  12. **4K 영상** — 1080p → 2160×3840 (세로) / 3840×2160 (가로)
  13. **4K 용량 오류 수정** — CRF 18→26으로 파일 크기 10-25MB로 감소
  14. **업로드 이미지 리사이즈** — convertToWebp에 max 2160px 캡 추가 (48MP 폰사진 오류 방지)
  15. **영상 생성 속도 3-4배 향상** — 클립은 1080p에서 처리, 최종 합성 시 4K 스케일업
  16. **Supabase 버킷 용량** — 50MB → 500MB (사용자 SQL 직접 실행)
  17. **백업** — gorang-ai-backup-20260619.tar.gz (114KB) 생성

- 변경된 파일:
  - `gorang-ai/app/page.js` — 'use client' + 자동 리다이렉트
  - `gorang-ai/app/home/page.js` — 채널 목록 7개, ytConnected 분리, 스마트 버튼
  - `gorang-ai/app/connect/page.js` — 구글 연동됨 상태 표시
  - `gorang-ai/app/api/shop/route.js` — google_connected 필드 추가
  - `gorang-ai/app/video/page.js` — 수정 패널, convertToWebp 리사이즈
  - `gorang-video-server/server.js` — 4K, 워터마크, CRF 26, 1080p 처리→4K 스케일업, fps 24, 3.0s
  - `gorang-video-server/Dockerfile` — fonts-noto-color-emoji 추가

- 결과: Vercel + Railway 배포 완료
- 커밋: c49e8c8 (gorang-ai), d2efa81 (gorang-video-server)

- 다음 할 일 (2026-06-20):
  1. **[최우선] 구글맵 악성리뷰 → 카톡 즉시알림 + 원탭 답변** — CLAUDE.md 1순위
  2. 4K 영상 실제 생성 테스트 (오류 없는지 확인)
  3. 틱톡 재연동 + 영상 업로드 테스트 (프로덕션 키)
  4. Vercel 환경변수 입력 (META_APP_ID, META_APP_SECRET, META_REDIRECT_URI, TRIPADVISOR_API_KEY)
  5. Supabase SQL 실행 (Meta/LINE/TripAdvisor 컬럼 추가, video_saves 테이블)
  6. 영업: 제주 카페 사장 3명에게 "월 3만원이면 쓸래요?" 지불의향 확인

---

## [2026-06-18] 영상 자막·제목 품질 대폭 개선 + 폰트 선택 UI ✅

- 한 일:
  1. **이중언어 자막 동시표시 버그 수정** — ASS Dialogue 단일 라인(\\N) → 별도 2개 라인으로 변경해 한국어·외국어가 같은 화면에 동시 표시
  2. **자막 위치 불일치 수정** — `alignSubToKo()` 함수 추가: 영어 문장 수가 한국어와 달라도 같은 사진에 자막이 맞게 배분됨
  3. **한국어 캡션 수정 시 자동 재번역** — `/api/caption/translate` 엔드포인트 신규 생성, "수정완료" 버튼 누르면 영/중/일 자동 갱신
  4. **주제목 변경 시 플랫폼 캡션 자동 초기화** — titleText 바뀌면 플랫폼별 캡션 stale 방지
  5. **주제목 스타일 고급화** — 폰트: Gowun Batang 고정, 흰색+검은두꺼운테두리(bord=10), 크기 업(120/90px)
  6. **주제목 폰트 선택 UI** — 영상 만들기 페이지에 7종 폰트 칩 선택 추가 (고운바탕/블랙한산스/프리텐다드/도현/주아/개구/나눔펜)
  7. **SSH 키 설정** — HTTPS 인증 실패 해결, GitHub SSH 키 등록 완료
  8. **텔레그램 봇 systemd 자동시작** — WSL 재시작 후에도 봇 자동 복구

- 변경된 파일:
  - `app/video/page.js` — 폰트 선택 UI, titleFont 상태, 자동재번역 로직, 플랫폼 캡션 초기화
  - `app/api/video/generate/route.js` — alignSubToKo() 추가, titleFont 파라미터 전달
  - `app/api/caption/translate/route.js` — 신규 (한→외국어 번역 엔드포인트)
  - `gorang-video-server/server.js` — 이중자막 동시표시, 주제목 스타일, titleFont UI 선택 지원
  - `gorang-video-server/Dockerfile` — 기존 폰트 7종 설치 확인
  - `/home/firebat/.config/systemd/user/telegram-bot.service` — 봇 자동시작

- 결과: Vercel + Railway 배포 완료 (커밋 ceff7ef, 5246135)
- 다음 할 일:
  - 추가 폰트 원하면 구글폰트 URL + 이름 알려주면 바로 추가
  - GBP API 승인 대기 → 승인 시 리뷰 폴링 + 원탭 답변 연결
  - 영업 실행 (제주 카페 사장 3명에게 데모)

---

## [2026-06-17] 솔라피 알림톡 발송 테스트 완료 ✅
- 한 일: 변수명 한국어 수정 (#{업체명}/#{별점}/#{작성자}/#{리뷰요약}/#{추천답변}), IP 제한 해제, templateId 필드명 수정, 전화번호 DB 저장
- 결과: 카톡 알림톡 정상 수신 확인 (내용 완전 채워짐)
- 남은 것: GBP API 승인 후 → 실제 리뷰 폴링 → 원탭 게시 연결 (버튼은 /review 페이지로 고정)
- 다음 할 일: GBP API 케이스 승인 대기 → 승인 시 실제 폴링 테스트

---

## [2026-06-17] 솔라피 알림톡 연동 코드 완성
- 한 일: `lib/solapi.js` 신규 생성 (sendBadReviewAlert/sendReviewAlert/sendWeeklyReminder), `app/api/reviews/poll/route.js` 에 알림 발송 로직 추가 + 모델명 수정 (`claude-sonnet-4-20250514` → `claude-sonnet-4-6`), `solapi` npm 패키지 설치 (v6.0.1)
- 변경된 파일: `lib/solapi.js` (신규), `app/api/reviews/poll/route.js`, `package.json`, `.env.example`
- 결과: 코드 완성. Vercel 환경변수 추가 + 템플릿 코드 입력 후 즉시 동작 가능
- 다음 할 일: ① Vercel에 SOLAPI_* 환경변수 추가 ② 솔라피 콘솔에서 발신번호 등록 + PFID 확인 ③ 템플릿 검수 승인 후 코드 입력 ④ users 테이블에 phone / kakao_notify 컬럼 확인

---

## [2026-06-17] 세션 기억 시스템 구축 완료
- 한 일: CLAUDE.md 세션 기억 규칙 섹션 추가 업데이트, 진행상황.md 생성, GitHub PAT 생성, Vercel 배포 준비 시작
- 변경된 파일: `CLAUDE.md` (세션 기억 규칙 추가), `진행상황.md` (신규 생성)
- 결과: 완료 — 다음 세션부터 컨텍스트 자동 유지됨
- 다음 할 일: Vercel 배포 연동 완료 → 1순위 GBP 악성리뷰 카톡 알림 기능 착수

---

## [2026-06-17] CLAUDE.md & 진행상황.md 세팅
- 한 일: 프로젝트 자동 브리핑용 CLAUDE.md 생성, 세션 기억용 진행상황.md 생성
- 변경된 파일: `CLAUDE.md`, `진행상황.md` (프로젝트 루트)
- 결과: 완료
- 다음 할 일: 1순위 구글맵 악성리뷰 → 카톡 알림 기능 착수

---

## 현재 상태 스냅샷 (2026-06-17 오후 업데이트)

### ✅ 오늘 완료한 것
- 솔라피 API Key / Secret 연동 (SOLAPI_API_KEY, SOLAPI_API_SECRET)
- `lib/solapi.js` 생성 — 악성/일반/주간 알림 3종 발송 함수
- 알림톡 템플릿 3종 코드 Vercel 환경변수 등록 완료
- `app/api/reviews/poll/route.js` — 신규 리뷰 감지 시 카톡 자동 발송 연결
- `app/api/test-notify/route.js` — 테스트 발송 엔드포인트
- 리뷰 페이지에 "🔔 새 리뷰 확인" 버튼 추가
- Supabase users/reviews 테이블 컬럼 추가 (phone, kakao_notify, star_rating 등)
- 솔라피 IP 제한 해제, templateId 필드명 수정, 변수명 한국어 수정
- **카톡 알림톡 실제 수신 확인 ✅** (업체명/별점/작성자/리뷰요약/AI답변 모두 정상)

### 🚧 GBP API 승인 대기 중 (케이스 9-3466000040455)
- 승인 나면 리뷰 자동 폴링 → 카톡 즉시 알림 풀 자동화 완성
- 원탭 게시: 버튼 클릭 → /review 페이지 → 최신 미답변 리뷰 자동 선택 방식으로 구현 예정

---

## 현재 상태 스냅샷 (2026-06-17 기준)

### ✅ 완료된 주요 기능
- Railway + FFmpeg 영상 서버 완전 구축 (sharp 정규화 + Ken Burns 줌인 4패턴 + 크로스페이드 0.8초 + LUT 색보정 + ASS자막 + BGM + 30fps)
- 감성 폰트 7종 설치 (Pretendard, Black Han Sans, Do Hyeon, Jua, Gowun Batang, Nanum Pen, Gaegu-Bold)
- 영상별 통합 색상/폰트 선택 (luminance 분석, resolveNanumPen() 런타임 폰트명 감지)
- 비동기 폴링 방식 (jobId 즉시 반환 → /api/video/status 3초마다 최대 6분 폴링)
- BGM 30개 (ocean/cafe/lofi/luxury/bright/night 태그별 5개씩)
- 사진+영상 혼합 합성 (mediaItems 2단계 렌더링)
- CS챗봇 (chatbot_logs 테이블 + app/help/page.js)
- 연속기록(streak) 기능 (upload_streaks + user_streak_summary 테이블)
- 틱톡 프로덕션 앱 Live (2026-06-11~), 환경변수 교체 + Redeploy 완료
- 카카오 채널 공개 + 검색허용 완료
- 큐레이션계정: 틱톡(hiddenjejutrip) bio 완료, 인스타(jejujwlife) bio 후보 작성

### ⏳ 외부 승인 대기 중
- 솔라피 알림톡 템플릿 3종 검수중 (gorang_bad_review_alert, gorang_review_alert, gorang_weekly_reminder)
- Meta 비즈니스 인증 검토중 → 완료 후 앱검수 제출 가능
- GBP API 케이스 9-3466000040455 이메일 대기중

### 🎯 다음 작업 우선순위
1. **[1순위] 구글맵 악성리뷰 → 카톡 즉시알림 + 원탭 답변**
   - 구현 순서: GBP 폴링 + Claude 분류/번역 → 카톡 연결 → 발송로직 + 원탭 딥링크 → 설정 UI
   - 알림톡 승인 대기 중이므로, 승인 전에 GBP 리뷰 폴링 코드부터 작업
2. 틱톡 재연동 + 업로드 테스트 (프로덕션 키)
3. 영업 실행: 제주 카페 사장 3명 지불의향 확인
4. 인스타 앱검수 제출 (Meta 인증 완료 후)
5. 월말 리포트 (GBP API 승인 후)
